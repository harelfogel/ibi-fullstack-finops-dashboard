# Backend

FastAPI service for transaction ingestion, validation, persistence, portfolio calculations, rule violations, and AI portfolio health insights.

Planned modules:

- `app/api/` - HTTP routes.
- `app/db/` - database setup and models.
- `app/services/` - portfolio, validation, violation, and insight services.
- `tests/` - focused unit tests for business logic.
